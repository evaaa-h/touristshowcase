package unit4;

import java.util.HashMap;
import java.util.Map;

public class MapExercise {
   static Map<String, User> database = new HashMap<>();

    static void main(){

        //Map
        //Key = Username
        //Value = User-Objekt, which contains attributes like Username, Password, Role

        //Add 3 Users to map / fake database
        //login-Method, which returns the user-object if exists, else null

      //  database.put("alice", new User("alice", "hallo123", "admin"));
        //database.put("bob", new User("bob", "ciao123", "user"));
        //database.put("chiara", new User("chiara", "bye123", "user"));


      // User u = login("alice");
     //  System.out.println(u);
      // if(u == null){
     //      System.out.println("User not found");
      // } else {
     //      System.out.println("Welcome" + u.username);
       }
    }
    //Login Methode
   // static User login(String username){
        //users.get(username);
      //  if (user != null && user.password.equals(login.password)){
      //      return user;
      //  }
    //    return null;
   // }
//}
